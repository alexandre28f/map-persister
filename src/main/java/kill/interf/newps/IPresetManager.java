package kill.interf.newps;

import java.io.Serializable;

import kill.util.IPrettyPrintable;

/**	Topmost level of preset management. (Much of the stuff below is going to be
 	purged.
 
	@author nick
 */

public interface IPresetManager extends Serializable, IPrettyPrintable {
//	void storeScalarChange(int param, float value);
//	void storeVectorChange(int param, float values[]);
}
